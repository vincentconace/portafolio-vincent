'use client';

import { styled } from 'styled-components';

export const MagneticItem = styled.span`
  position: relative;
  z-index: 1;
  display: block;
  width: max-content;
  white-space: nowrap;
`;
